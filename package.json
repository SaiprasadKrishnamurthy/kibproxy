{
  "name": "kibproxy",
  "version": "1.0.0",
  "description": "a proxy for kibana to apply sticky magic filters by the calling apps transparent to the kibana users",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "saiprasad krishnamurthy",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.3",
    "express": "^4.14.0",
    "express-http-proxy": "^0.10.0"
  }
}
